import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  scrollContainer: {
    paddingBottom: 30,
  },
  bannersList: {
    marginTop: 18,
  },
  serialsList: {
    marginTop: 32,
  },
});
