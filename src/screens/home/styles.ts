import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  scrollContainer: {
    paddingBottom: 30,
  },
  listBanners: {
    marginTop: 18,
  },
  listSerials: {
    marginTop: 32,
  },
});
