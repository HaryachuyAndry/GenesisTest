export type SerialItemType = {
  id: string;
  url: string;
  title: string;
  subtitle: string;
  available: string;
  genre: string;
};

export type EpisodeType = {
  title: string;
  id: string;
  url: string;
};
